[project]
name = "longitrack-backend"
version = "0.1.0"
description = "GPU backend for point-prompted longitudinal lesion tracking with LongiSeg: model loading, registration, and segmentation, served over a local or remote socket."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10,<3.13"
authors = [
    { name = "Yannick Kirchhoff", email = "yannick.kirchhoff@dkfz-heidelberg.de" },
]
keywords = [
    "longitudinal",
    "lesion tracking",
    "medical image segmentation",
    "LongiSeg",
    "nnU-Net",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Healthcare Industry",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Scientific/Engineering :: Medical Science Apps.",
]

dependencies = [
    # model distribution
    "huggingface-hub>=0.24",
    # io / numerics
    "SimpleITK>=2.2.1",
    "numpy>=1.24",
    # remote-backend authentication
    "cryptography>=44",
]

[project.optional-dependencies]
# actually running inference/registration/the server
gpu = [
    # tracking model + registration, see [tool.uv.sources] below
    "longiseg[tracking]",
    # cc3d is imported by longiseg.tracking but missing from its dependency list
    "connected-components-3d>=3.12",
    "torch>=2.1.2",
]
dev = [
    "pytest>=8",
    "ruff>=0.6",
]

# installed by default in this clone; downstream installs stay lightweight unless they ask
[dependency-groups]
gpu = [
    "longiseg[tracking]",
    "connected-components-3d>=3.12",
    "torch>=2.1.2",
]

[tool.uv]
default-groups = ["gpu"]

[project.urls]
homepage = "https://github.com/MIC-DKFZ/LongiTrack-backend"
repository = "https://github.com/MIC-DKFZ/LongiTrack-backend"
"LongiSeg" = "https://github.com/MIC-DKFZ/LongiSeg"

[project.scripts]
longitrack-backend = "longitrack_backend.server:main"
longitrack-model = "longitrack_backend._cli:main"
create_remote_id = "longitrack_backend.remote_identity:create_main"
push_remote_id = "longitrack_backend.remote_identity:push_main"
create_and_push_remote_id = "longitrack_backend.remote_identity:create_and_push_main"

# --- uv -----------------------------------------------------------------------
# LongiSeg is not on PyPI, so it is pulled straight from the GitHub remote. To work
# against a local clone instead, either edit this to
#     longiseg = { path = "../longi-seg-pub", editable = true }
# or, without touching this file, run
#     uv sync && uv pip install -e ../longi-seg-pub
[tool.uv.sources]
longiseg = { git = "https://github.com/MIC-DKFZ/LongiSeg.git", branch = "master" }

# torch resolves to the CUDA-enabled manylinux wheel from PyPI by default. For a
# CPU-only install (much smaller), uncomment the following and add the index:
# [[tool.uv.index]]
# name = "pytorch-cpu"
# url = "https://download.pytorch.org/whl/cpu"
# explicit = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/longitrack_backend"]

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]
ignore = ["B008"]

[tool.pytest.ini_options]
testpaths = ["tests"]
